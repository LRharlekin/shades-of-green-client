import { cleanup } from '@testing-library/react';

describe('Filters', () => {
  afterEach(cleanup);

  it('should render a button for each element in the array', () => {

  })
})

//TODO: TEST
//RENDER THE COMPONENT AND THEN USE SOMETHING TO ACCESS THE NAME OF EACH BUTTON TO CHECK THAT THERE IS ONE FOR EACH VALUE PROVIDED